{
  "build": {
    "sourceDir": "templates",
    "outputDir": "dist",
    "cleanOutput": true
  },
  "targets": [
    {
      "id": "copilot",
      "name": "GitHub Copilot",
      "bundleName": "copilot.zip",
      "format": "markdown",
      "configDir": ".github",
      "directories": {
        "commands": ".github/prompts",
        "skills": ".github/skills",
        "guides": ".github/apm-guides",
        "agents": ".github/agents"
      },
      "contextAttachSyntax": "Reference the file using `#file` and select the file from the picker.",
      "newChatGuidance": "Start a new Copilot Chat",
      "subagentGuidance": {
        "hasSubagents": true,
        "toolSyntax": "#runSubagent",
        "explorerName": "exploration subagent",
        "configNote": null
      },
      "workerDispatchGuidance": "Spawn the Worker as a subagent using `#runSubagent` with the `apm-worker` agent. Pass the complete Task Prompt as the subagent's instructions. The Worker executes in its own context and returns results when complete.",
      "workerDeliveryStandards": "Spawn the Worker subagent with the Task Prompt as its instructions per §2.4 Dispatch Standards. For batch dispatch, include all Task Prompts in the subagent's instructions separated by `---` delimiters. The subagent returns results directly - no bus clearing or file writing needed.",
      "workerReportIntake": "The Worker subagent returns its report directly as its completion response. Parse the status, log path, and findings from the response.",
      "postInstallNote": null
    },
    {
      "id": "claude",
      "name": "Claude Code",
      "bundleName": "claude.zip",
      "format": "markdown",
      "enhancedSkills": true,
      "configDir": ".claude",
      "directories": {
        "commands": ".claude/commands",
        "skills": ".claude/skills",
        "guides": ".claude/apm-guides",
        "agents": ".claude/agents"
      },
      "contextAttachSyntax": "Reference the file using `@` followed by the file path.",
      "newChatGuidance": "Open a new terminal and start Claude Code",
      "subagentGuidance": {
        "hasSubagents": true,
        "toolSyntax": "Agent(subagent_type=\"Explore\", prompt=\"...\")",
        "explorerName": "Explore",
        "configNote": null
      },
      "workerDispatchGuidance": "Spawn the Worker as a subagent using `Agent(subagent_type=\"apm-worker\", prompt=\"...\")`. Control the model per Task: `model=\"sonnet\"` for standard work, `model=\"opus\"` for complex tasks. For parallel dispatch, add `isolation=\"worktree\"` to give each Worker an isolated workspace. The Worker executes in its own context and returns results when complete.",
      "workerDeliveryStandards": "Spawn the Worker subagent with the Task Prompt as its instructions per §2.4 Dispatch Standards. For batch dispatch, include all Task Prompts in the subagent's instructions separated by `---` delimiters. The subagent returns results directly - no bus clearing or file writing needed.",
      "workerReportIntake": "The Worker subagent returns its report directly as its completion response. Parse the status, log path, and findings from the response.",
      "postInstallNote": null
    },
    {
      "id": "gemini",
      "name": "Gemini CLI",
      "bundleName": "gemini.zip",
      "format": "toml",
      "configDir": ".gemini",
      "directories": {
        "commands": ".gemini/commands",
        "skills": ".gemini/skills",
        "guides": ".gemini/apm-guides",
        "agents": ".gemini/agents"
      },
      "contextAttachSyntax": "Reference the file using `@` followed by the file path.",
      "newChatGuidance": "Open a new terminal and start Gemini CLI",
      "subagentGuidance": {
        "hasSubagents": true,
        "toolSyntax": "@codebase_investigator <natural language query>",
        "explorerName": "codebase_investigator",
        "configNote": "Requires `experimental.enableAgents: true` in ~/.gemini/settings.json"
      },
      "workerDispatchGuidance": "Spawn the Worker as a subagent using `@apm-worker` followed by the Task Prompt content. The Worker executes in its own context and returns results when complete. Alternatively, the main agent can invoke it automatically when the `apm-worker` agent description matches the task.",
      "workerDeliveryStandards": "Spawn the Worker subagent with the Task Prompt as its instructions per §2.4 Dispatch Standards. For batch dispatch, include all Task Prompts in the subagent's instructions. The subagent returns results directly - no bus clearing or file writing needed.",
      "workerReportIntake": "The Worker subagent returns its report directly as its completion response. Parse the status, log path, and findings from the response.",
      "postInstallNote": "Gemini CLI requires `experimental.enableAgents: true` in ~/.gemini/settings.json for subagent features."
    },
    {
      "id": "cursor",
      "name": "Cursor",
      "bundleName": "cursor.zip",
      "format": "markdown",
      "configDir": ".cursor",
      "directories": {
        "commands": ".cursor/commands",
        "skills": ".cursor/skills",
        "guides": ".cursor/apm-guides",
        "agents": ".cursor/agents"
      },
      "contextAttachSyntax": "Reference the file using `@` and search for the file name.",
      "newChatGuidance": "Start a new Cursor Agent chat",
      "subagentGuidance": {
        "hasSubagents": true,
        "toolSyntax": "Task(subagent_type=\"explore\", prompt=\"...\")",
        "explorerName": "explore",
        "configNote": null
      },
      "workerDispatchGuidance": "Write the Task Prompt to the Worker's Task Bus at `.apm/bus/<agent-slug>/task.md`. Direct the User to the Worker session to run `/check-tasks`. If the Worker is not yet initialized, direct the User to start a new chat and run `/initiate-worker <agent-id>` first.",
      "workerDeliveryStandards": "Before writing to a Worker's Task Bus, clear the Worker's Report Bus (`.apm/bus/<agent-slug>/report.md`) via terminal (e.g., `truncate -s 0` or shell redirection). Skip clearing on first Task Prompt to a Worker when no report exists.\n\nWhen dispatching multiple sequential Tasks to the same Worker, send them as a batch in a single Task Bus message per `{SKILL_PATH:apm-communication}` §4.5 Batch Envelope Format.",
      "workerReportIntake": "Read the report from the Report Bus (`.apm/bus/<agent-slug>/report.md`). The User delivers the report by running `/check-reports` in this chat.",
      "postInstallNote": null
    },
    {
      "id": "opencode",
      "name": "OpenCode",
      "bundleName": "opencode.zip",
      "format": "markdown",
      "configDir": ".opencode",
      "directories": {
        "commands": ".opencode/commands",
        "skills": ".opencode/skills",
        "guides": ".opencode/apm-guides",
        "agents": ".opencode/agents"
      },
      "contextAttachSyntax": "Reference the file using `@` followed by the file name.",
      "newChatGuidance": "Open a new terminal and start OpenCode",
      "subagentGuidance": {
        "hasSubagents": true,
        "toolSyntax": "Task(subagent_type=\"explore\", prompt=\"...\")",
        "explorerName": "explore",
        "configNote": null
      },
      "workerDispatchGuidance": "Write the Task Prompt to the Worker's Task Bus at `.apm/bus/<agent-slug>/task.md`. Direct the User to the Worker session to run `/check-tasks`. If the Worker is not yet initialized, direct the User to start a new chat and run `/initiate-worker <agent-id>` first.",
      "workerDeliveryStandards": "Before writing to a Worker's Task Bus, clear the Worker's Report Bus (`.apm/bus/<agent-slug>/report.md`) via terminal (e.g., `truncate -s 0` or shell redirection). Skip clearing on first Task Prompt to a Worker when no report exists.\n\nWhen dispatching multiple sequential Tasks to the same Worker, send them as a batch in a single Task Bus message per `{SKILL_PATH:apm-communication}` §4.5 Batch Envelope Format.",
      "workerReportIntake": "Read the report from the Report Bus (`.apm/bus/<agent-slug>/report.md`). The User delivers the report by running `/check-reports` in this chat.",
      "postInstallNote": null
    }
  ]
}
